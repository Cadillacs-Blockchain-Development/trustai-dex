import { Aleo, Poppins, Rubik } from "next/font/google";

export const aleo = Aleo({ subsets: ["latin"] });
export const poppins = Poppins({ subsets: ["latin"], weight: ["600", "400"] });
export const rubik = Rubik({ subsets: ["latin"] });
